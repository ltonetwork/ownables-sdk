name: Build and Deploy
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
      - name: Install and Build 🔧
        working-directory: ./www
        run: |
          npm ci
          ls
          cd ../ownables
          ls
#          cp -a assets styles.css dist
#       - name: Load Ownable Packages
#         working-directory: ./
#         run: |
#           mv ownables/*.zip www/dist
#           ls www/dist
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: www/dist
